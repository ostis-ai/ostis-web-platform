1 | test | 667;;
5 -> 6: 7: tr; ty;;
6 <- 8: re; gg; hj;;

file -> "file://image.png";;
set -> attr_set: { sum: 1; result: 2; 3};;
set ~> < attr1: 1; attr2: 2; attr3: 3>;;

el1 _<=> el2;;

file => nrel_system_identifier: ["fs_file"] (* <- string; russian;; <~ opened;;*);;

_contour -> [*

	a -> b;;
	v -> c (* <- d;; *);;
	.cnt -> g;;
*];;

"file://image.png" <- format_png;;

contour = [* test -> y;; *] (* <- x;; *);;
contour <- y;;




Partition_SCs_code <= nrel_decomposition: 
 {
  Partition_SCs_code_1_level;
  Partition_SCs_code_2_level;
  Partition_SCs_code_3_level;
  Partition_SCs_code_4_level;
  Partition_SCs_code_5_level;
  Partition_SCs_code_6_level;
  Partition_SCs_code_7_level;
  Partition_scs_divider
 };;
 
 "file://image.png" = y;;
 
 Partition_SCs_code_1_level = 
 [* x -> y;; *];;
 
 
 y -> [*^"file://incl.scsi"*];;

const_arc -> (v -> (g <= n)) (* <- tested;; *);;